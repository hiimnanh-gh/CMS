@model List<CMS.Models.ShowTimeViewModel>

<h2>Chọn Giờ Chiếu</h2>

<ul>
    @foreach (var time in Model)
    {
        <li>
            <a href="@Url.Action("SelectSeats", "Booking", new { timeId = time.TimeId })">
                @time.ShowingDatetime
            </a>
        </li>
    }
</ul>
